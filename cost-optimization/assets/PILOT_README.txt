Elastic AI Performance (internal pilot)
========================================

You already unzipped (or downloaded from GitHub) — good. Follow the steps below.

Use Claude (recommended)
------------------------
1. In this same folder, open **ELASTIC_PILOT_PROMPT.txt** (small pilot zip at top level) **or** **elastic-pilot-participant-prompt.txt** in **assets/** (GitHub zip under **skills/cost-optimization/** — same instructions).
2. Select **all** the text, copy it.
3. Open **Claude Desktop** or **Claude Code**. Start a **new chat**. Paste what you copied. Attach our zip in that chat **if you still have it** (if not, the pasted text tells Claude how to use GitHub instead).
4. When Claude is finished, it should show you where a file named **FEEDBACK.md** was saved. Send that file to your coordinator (that is the report). Same folder may have SLACK.md and PILOT_ROLLUP.json — send those too if Claude gives them to you.

You are not expected to type commands yourself; Claude does the technical steps.

Only have an **ai-performance-skills** GitHub ZIP?
-----------------------------------------
Open **skills/cost-optimization/assets/** — use **START_HERE.txt**, **PILOT_README.txt** (this file’s copy there), and **elastic-pilot-participant-prompt.txt** for the Claude step (same flow as the small zip).

Day 30 follow-up (only when asked)
-----------------------------------
From the folder that has **.ai-cost-data/**, your assistant runs **pilot-30d-report.sh** from the installed skill; return the new **FEEDBACK.md** the same way your coordinator described.

No MCP server is required. Nothing is sent automatically.
